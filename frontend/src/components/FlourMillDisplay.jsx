import React from 'react'
import { DynamicMasterDisplay } from './master/DynamicMasterDisplay'

const FlourMillDisplay = () => {
  return <DynamicMasterDisplay configKey="flour_mill" />
}

export default FlourMillDisplay
